# 2026-07-28 工作日志

## 持久化记忆整理
- 将用户提供的27条历史记忆整理并写入持久化存储（update_memory工具），合并为8条结构化记忆
- 同步创建MEMORY.md长期记忆文件，涵盖项目概述、服务器配置、开发环境、PocketBase规范、PowerShell注意事项、Caddy/CSP配置、ESA标头、构建命令、代码规范、核心功能

## 子智能体配置
- 创建了4个project级子智能体，存储于 `.codebuddy/agents/` 目录：
  1. `code-reviewer.md` — 代码审查专家，模型xopglm52，agentic模式
  2. `security-auditor.md` — 安全审计专家，模型xopglm52，agentic模式
  3. `pb-migration-expert.md` — PocketBase迁移专家，模型hy3，agentic模式
  4. `frontend-ui-expert.md` — 前端UI专家，模型hy3，agentic模式
- 所有子智能体均开启enabledAutoRun，工具按最小化原则配置
- 子智能体可单独配置model字段指定不同模型
- 第二批新增4个子智能体：devops-deployer(xopglm52)、test-writer(hy3)、pb-hooks-expert(xopglm52)、seo-content-expert(hy3)
- 共8个子智能体覆盖：代码审查、安全审计、PB迁移、前端UI、运维部署、测试编写、PB hooks、SEO内容
- 第二批新增8个子智能体：code-explorer(xopglm52)、debugger(xopglm52)、api-designer(xopglm52)、refactoring-expert(xopglm52)、documentation-writer(hy3)、performance-optimizer(xopglm52)、content-writer(hy3)、git-workflow(hy3)
- 共16个子智能体，覆盖完整开发链路：探索→设计→编码→测试→审查→安全→部署→运维
- 注意：自定义code-explorer会与内置code-explorer竞争，但自定义的description包含中文关键词，应优先匹配中文场景
- 主模型为k3-256k，子智能体模型为xopglm52(5个)和hy3(3个)
- xopglm52和hy3只允许temperature=1，需在frontmatter中显式设置temperature: 1，否则报400错误
- 实际解决方案：在models.json中为k3-256k和xopglm52添加temperature: 1字段，子智能体frontmatter中不需要temperature字段
- models.json路径：C:\Users\ROG\.codebuddy\models.json
- 注意：子智能体frontmatter中hy3模型名称无效，实际可用模型为xopglm52和k3-256k

## 全面阅读项目（结构梳理）
- 通过 code-explorer 子代理完成对 astro/、pb_hooks/、pb_migrations/、docs/、scripts/、tests/、admin-auth/ 的全面结构梳理
- 关键发现：
  - astro/ 不使用 Astro Content Collections，文章全部由 PocketBase 承载，构建时 getStaticPaths 拉取 slug 静态生成
  - 数据分层模式：service（PB CRUD）→ domain hook（状态）→ React 组件（UI），services 25个、domain hooks 25个
  - admin-auth/ 是独立 Node 微服务（端口8787，仅内网），负责管理员认证策略与供应商中立邮件网关（HMAC验签）
  - pb_hooks 共26个启用hook+27个lib库，顶层hook只注册路由/cron，业务逻辑全部在lib/内require
  - 邮件平台是第二大子系统：网关→outbox队列→归档(rclone+age加密)→模板/日志/审计
  - 四层安全防护链：OpenResty限频→Caddy封禁/日志脱敏→PB hooks(RBAC/TOTP step-up/审计/限频)→持久化security_rate_limits
  - 隐私设计：统计不存原始IP（HMAC按日轮换visitor_hash），地理只存区划代码
  - login_security.pb.js 生产强制禁用（改用OpenResty限频），与 CLAUDE.md 中"已重新启用"的描述存在出入，以 security-check.sh 校验为准
  - geo_debug.pb.js 是临时诊断端点，注释标注"验证后删除"

## 排查：访问统计页面空白（已定位根因）
- 现象：生产 https://hlydwz.com/stats/ 统计区域空白
- 排查结论：
  - 生产 API `/api/blog-stats?range=30d` 正常（285次访问/84访客，数据自07-23起采集）
  - 前端 StatsDashboard/useStats/statsService 代码无问题；sw.js 不缓存 HTML 无责
  - **根因：ESA 边缘缓存无视源站 no-store，分发 47 小时前的旧 HTML（Age: 169812）；当天 13:15 部署（tmp/deploy_20260728_131513）后旧 hash chunks 被删除，旧 HTML 引用的 StatsDashboard.DGm3cATc.js 等 6/13 chunks 404 → 岛屿无法 hydration → 空白**
  - 带随机 query 绕过边缘后源站 HTML 完好（StatsDashboard.Dp48TNG9.js 等 13/13 全部 200）
- 修复动作（需用户在阿里云 ESA 控制台执行）：刷新缓存（立即）+ 配置缓存规则 HTML 不缓存/遵循源站（根治，否则每次部署复发）
- 已将 ESA HTML 缓存问题写入 MEMORY.md

## 新功能设计：后台 ESA 缓存刷新（头脑风暴完成，待用户确认设计）
- 需求确认（用户逐项选择）：
  1. 调用阿里云 ESA OpenAPI PurgeCaches（2024-09-10），用户最初说"云效API"实为口误
  2. 刷新粒度：全站（purgeall）+ 自定义路径（file/directory，多行 URL）
  3. 凭证：后台手动添加，加密保存（复用 admin_totp.js encryptSecret，同 mail_smtp_config 模式）
  4. 权限：**仅 super_admin**（用户后补充强调），前端二次确认，后端限频
  5. 入口：独立「缓存管理」页
  6. 用户补充要求：检查普通用户不可见不应展示的界面（三层兜底：侧栏 requiredRole 过滤 + AdminGuard + 后端 requireSuperAdmin）
- 架构选型：方案B——经 admin-auth 微服务代理调 ESA（复用 mail_gateway HMAC 设施，Node crypto 原生支持阿里云 RPC HMAC-SHA1 签名；PB JSVM $security 无 SHA1，直调方案被否）
- 关键 API：ESA PurgeCaches（SiteId/Type/Content→TaskId）、DescribePurgeTasks（状态查询），endpoint esa.cn-hangzhou.aliyuncs.com，RPC 风格
- 设计要点：esa_purge_settings（单条加密凭证集合）+ esa_purge_tasks（记录集合）+ cache_admin.pb.js + admin-auth 新增 esa-client.mjs + /internal/esa/* 路由 + 前端 CacheManager 组件 + AdminSidebar 新增项
- 用户确认方案B，spec 已落盘：docs/superpowers/specs/2026-07-28-esa-cache-purge-design.md（含数据层/后端/admin-auth/前端/安全清单/测试计划/部署步骤）

## ESA 缓存刷新功能实施完成（同日）
- spec 评审通过后完成全部实施，新增/修改文件：
  - 迁移：pb_migrations/20260728120000_create_esa_purge_collections.pb.js（esa_purge_settings 单条加密凭证 + esa_purge_tasks 记录，均无 API 规则仅 hook 访问）
  - PB hooks：cache_admin.pb.js（4 路由）+ lib/cache_admin.js（业务）+ lib/cache_esa_config.js（encryptSecret 加密读写）+ lib/cache_esa_gateway.js（复用 mail_crypto.sign 内网调用）
  - admin-auth：src/esa/client.mjs（阿里云 RPC HMAC-SHA1 签名 + PurgeCaches/DescribePurgeTasks）+ src/esa/http.mjs（复用 mail 通道 HMAC 验签）+ server.mjs 接线 + test/esa/client.test.mjs
  - 前端：pages/admin/cache.astro + components/admin/cache/EsaCacheManager.tsx + hooks/domains/useEsaCache.ts + lib/services/esaCacheService.ts + AdminSidebar 新增「缓存管理」项（super_admin）
- 关键技术决策：
  - 阿里云 RPC 签名 V1（HMAC-SHA1）在 admin-auth Node 侧实现；ESA endpoint esa.cn-hangzhou.aliyuncs.com，API 2024-09-10
  - 内网通道复用 MAIL_INTERNAL_SECRET 验签（X-Mail-* 头），无新增环境变量
  - purge URL 白名单限定 PUBLIC_SITE_URL 域名（fallback hlydwz.com），/结尾归 directory 其余归 file
  - 限频复用现有 admin_security_write 策略桶（未新增 policy key）
  - 可见性三层：AdminSidebar hasPermission 过滤 + AdminGuard requiredRole 重定向 + 后端 requireSuperAdmin（用户特别关注普通用户不可见）
- 验证结果：admin-auth 178 测试全过（10 个新增 ESA 测试）；PB hooks/迁移 node --check 通过；astro 构建 Complete（/admin/cache/index.html 生成）；tsc 无相关错误
- 待办：本地环境（Docker）未运行，迁移未实机应用；生产部署需重建 admin-auth 镜像并在缓存管理页录入 RAM 子账号 AK（仅授权 esa:PurgeCaches/DescribePurgeTasks）+ SiteId
